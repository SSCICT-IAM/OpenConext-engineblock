<?php /* This file is generated. Please edit the files of the appropriate theme in the 'theme/' directory. */ ?>
<?php
/**
 * @var Zend_Layout $layout
 */
$layout = $this->layout();
$layout->assign('subheader', $this->t('cookie_removal_header'));
?>
<div class="box">
  <div class="mod-content">
    <h1><?= $this->t('cookie_removal_header'); ?></h1>
    <?= $this->t('cookie_removal_description'); ?>
    <form id="remove_cookie_form" action="#" method="post" class="apply clearfix">
      <button name="remove_cookie" id="remove_cookie_submit" class="c-button"><?= $this->t('cookie_removal_header'); ?></button>
    </form>
<?php
  if (isSet($_POST['remove_cookie'])) {
    if (isSet($_COOKIE['main'])) {
        setcookie('main', null, -1, '/');
    }

    if (isSet($_COOKIE['selectedidp'])) {
        setcookie('selectedidp', null, -1, '/');
    }

    if (isSet($_COOKIE['selectedidps'])) {
        setcookie('selectedidps', null, -1, '/');
    }

    // Clear all session-data on the server
    session_start();
    session_destroy();

    echo "<div class='results'><br /><p style='color:red'>" . $this->t('cookie_removal_confirm') . "</p></div>";
  }
?>
  </div>
</div>
